import 'dart:typed_data';

Future<void> saveFileBytes({
  required Uint8List bytes,
  required String fileName,
}) async {
  throw UnsupportedError('Plataforma no soportada para guardado de archivos.');
}
